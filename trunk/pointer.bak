uses mcommon,crt;
var
f:file;
a:char;
w:string;
begin
clrscr;
if (isgetoptionvalue('d:*',w)) then
   begin
   writeln('tru');
   while (isgetoptionvalue('d:*',w)) do
         writeln('tru');
   end;
writeln(isgetoption('debug'));
end.